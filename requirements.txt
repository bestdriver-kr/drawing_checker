PySide6>=6.6
Pillow>=10.0
pikepdf>=8.0          # PDF 레이어(OCG) 내보내기
pypdfium2>=4.0        # PDF 열기(페이지 래스터화)
# 마킹검사(OCR)용 — 엔진은 선택해서 쓰며, 필요한 것만 설치하면 됩니다.
numpy>=1.24
easyocr>=1.7                 # EasyOCR 엔진(torch 포함, 용량 큼)
rapidocr_onnxruntime>=1.3    # RapidOCR 엔진(ONNX, 가벼움)
pytesseract>=0.3             # Tesseract 엔진(별도 tesseract.exe 설치 필요)
# Windows OCR 엔진은 winrt 사용(별도 설치 불필요, Windows 한정)
# Drawing OCR(도면 치수 전용) 엔진은 동봉된 drawing_ocr/ 패키지 + torch 사용
